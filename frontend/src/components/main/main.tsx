import React from 'react';
import Home from '../home-page/home_page'

interface MainProps {

}

const Main: React.FC<MainProps> = () => {
    return (
        <div>
            <Home />
        </div>
    );
};

export default Main;